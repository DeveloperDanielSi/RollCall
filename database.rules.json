{
  "rules": {
    ".read": "auth != null",   // Allow read access only to authenticated users
    ".write": "auth != null",  // Allow write access only to authenticated users
    "classes": {
      ".indexOn": ["creatorEmail"] // Indexing for optimized queries on the classes node
    }
  }
}
